// import { ActiveUserModule } from '@/store/modules/activeUserModule'
//
//
// let navMenuItems = [ ...publicNavMenuItems, ...loginMenuItems]
// if (ActiveUserModule.activeUserInfo) {
//   const admin = Object.values(ActiveUserModule.activeUserInfo.roles).filter(item => item === 'Admin')
//   navMenuItems = admin.length === 1 ? [...adminMenuItems] : [...publicNavMenuItems, ...profileMenuItems]
// }
//
// export default navMenuItems
